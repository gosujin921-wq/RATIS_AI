# KLID 포털 디자인 스타일 기준

KRDS 컴포넌트를 테마로 조정할 때 참고하는 여백·비례 기준 문서.
출처: 목업 `/style` 스타일 가이드 및 `klid_2nd/src/mockup/components/ui` 실측 (2026-07-22).

## 이 문서를 읽는 법

KRDS 는 컴포넌트마다 사이즈 변형(small~xlarge)이 있어서 px 하나만 적으면 "어느 사이즈의 값인지"가
애매해진다. 그래서 모든 항목을 두 층으로 적는다.

1. **비례 규칙** : 사이즈가 바뀌어도 유지되는 관계(비율·고정값·위계). KRDS 의 어떤 사이즈 변형에도
   이 규칙으로 값을 도출할 수 있다.
2. **실측 앵커** : 기준 px 표. 도출한 값이 맞는지 검증할 때, 그리고 높이가 같은 사이즈에는
   그대로 쓴다.

적용 순서: ① 높이가 스케일(36/44/52)과 같은 사이즈면 앵커 px 그대로 → ② 다른 높이면 비례 규칙으로
도출 → ③ KRDS 토큰이 사이즈 구분 없는 단일값이면 md 앵커를 쓴다.

- KRDS 는 `1rem = 10px`. 이 문서의 px 는 /10 해서 rem 으로 적는다 (예: 44px → 4.4rem).
- 이미 이식 완료된 값은 [src/styles/krds-theme.css](src/styles/krds-theme.css)가 정본.
  이 문서는 그 값들의 **비례 근거**와, 새 컴포넌트·화면에서 값을 정하는 규칙을 담는다.
- 최종 검증은 수치가 아니라 스토리북(6007) 실물 렌더 확인.

---

## 1. 전체 비례 문법 (대원칙 5)

화면 전체를 관통하는 여백 관계. 개별 수치가 기억나지 않아도 이 다섯으로 복원할 수 있다.

1. **컨트롤 높이는 한 스케일** : 버튼·입력·검색창·셀렉트가 36 / 44 / 52 (+8 등차) 하나를 공유한다.
   스케일 밖 높이(40, 48 등)를 쓰면 나란히 놓인 요소와 어긋난다. 예외는 밀집 UI 의 컴팩트 셀렉트(36 고정)와
   정사각 아이콘 버튼(28/32/36, 별도 스케일).
2. **컨트롤 글자는 높이의 약 1/3** : 36→14px, 44→15px, 52→17px. 세로 여백은 따로 주지 않고
   높이 고정 + 중앙 정렬로 만든다.
3. **가로 여백은 "클릭할 수 있을수록, 클수록" 넉넉하게** : 같은 캡슐이라도 정보 배지 < 필터 칩 < 버튼
   순으로 좌우 패딩이 커진다 (자기 글자크기 대비 0.77em → 1em → 1.33em). 버튼은 사이즈가 한 단계
   오를 때 패딩 비율 자체가 커진다 (아래 §3).
4. **표면 패딩은 라운드보다 크게** : 카드 라운드 16px 에 패딩 20~24px. 패딩 ≈ 라운드 × 1.25~1.5 를
   지키면 모서리 곡선에 내용물이 끼어 보이지 않는다.
5. **간격은 관계가 멀수록 커진다** : 한 요소 안의 조각 6~8 → 형제 칩·버튼 6~8 → 카드 사이 16 →
   컬럼 사이 24 → 블록 사이 20 → 구획 사이 32~48 → 페이지 섹션 80. 라벨과 그 대상은 밀착(4~6px),
   구획 경계는 그 5~10배로 벌린다.

---

## 2. 타이포그래피

스케일 (본문 15 기준, 하한 13 · 12px 이하 금지):

| px | 행간 | 쓰임 |
|---|---|---|
| 13 | 1.5 | 배지·카운터·메타 (하한) |
| 14 | 1.55 | 폼 라벨·칩·표 헤더·보조 본문 |
| 15 | 1.6 | 본문 기본·버튼 md·표 셀 |
| 17 | 1.55 | 큰 본문·카드 제목·버튼 lg |
| 20 | 1.4 | 상세 구획 제목 |
| 24 | 1.35 | 페이지 제목 (LNB 안쪽·폼 페이지) |
| 30 | 1.3 | 페이지 제목 (풀폭) |
| 40 | 1.2 | 빅넘버·메인 제목 |
| 52 | 1.15 | 히어로 |

- **행간은 크기와 반비례** : 본문권(13~17)은 1.5~1.6, 제목권(20~30)은 1.3~1.4, 디스플레이(40+)는 1.15~1.2.
- 자간 전역 -0.5px (Pretendard 한글 보정). 큰 제목·빅넘버는 tracking-tight 추가.
- **서체 스택** (2026-07-29 확정): 라틴·숫자 **Roboto** / 한글 **Pretendard GOV**.
  `--krds-typo-font-type: Roboto, 'Pretendard GOV'` 하나로 제어 — Roboto 에 한글 글리프가 없어 자동 폴백.
  로드는 index.html·preview-head.html (Roboto 400~800).
- **웨이트 사다리 — 상한 600 (2026-07-29 결정: 700+ 볼드 미사용)** : 400 본문 /
  500 보조 강조(배지·표 헤더·GNB·LNB 항목) / 600 강조 전부(버튼·칩·라벨·링크·제목·빅넘버).
  **배지는 500 이다** (2026-08-25) — 훑는 표식이라 강조 축에 두지 않는다.
  위계는 굵기 대신 크기·색으로 만든다. 킷의 bold 참조 48곳은 `--krds-font-weight-bold: 600`
  재정의로 일괄 적용(krds-theme.css). 예외는 브랜드 로고타입(Logo) 하나뿐.
  굵기를 색보다 먼저 올리지 않는다 — 표 헤더처럼 죽일 요소는 굵기(500)보다 색(slate-500)으로
  죽인다 (400 은 대비 미달 — §8 하한).
- 숫자는 항상 tabular-nums (표·카운터·빅넘버·페이저).
- **소제목(구획 제목)은 20 · 600** (2026-08-03 확정). 다는 기준은 **독립적으로 성립하는 기능 블록이
  둘 이상**일 때다 (문의하기의  / `내 문의`). 하나의 흐름을 이루는 조각에는 달지 않는다
  — 검색→결과, 필터→목록에서 필터는 아래 목록을 위한 도구지 별개 블록이 아니다.
  소제목을 달지 않는 영역도 `aria-label` 은 둔다.

---

## 3. 컨트롤 (버튼 · 입력 · 검색 · 셀렉트)

실측 앵커:

| | 높이 | 좌우 패딩 | 글자 | 아이콘 간격 |
|---|---|---|---|---|
| 버튼 sm | 36 | 14 | 14 | 6 |
| 버튼 md | 44 | 20 | 15 | 8 |
| 버튼 lg | 52 | 28 | 17 | 8 |
| 입력 (전 사이즈) | 36/44/52 | **14 고정** | 14/15/17 | - |
| 검색창 | 36/44/52 | 좌 44 · 우 16 | 15 | 아이콘 18, 좌측 16 위치 |
| 컴팩트 셀렉트 | 36 | 12 | 14 | 화살표 16 |

비례 규칙:

- **버튼 좌우 패딩은 높이 비례가 아니라 누진** : 높이 대비 0.39 → 0.45 → 0.54.
  큰 버튼일수록 글자 좌우가 상대적으로 더 널널하다. 자기 글자크기 기준으로는 1.0em → 1.33em → 1.65em.
  새 사이즈가 필요하면 이 누진 곡선 위에서 잡는다.
- **입력은 반대로 좌우 패딩 14px 고정** : 사이즈가 바뀌어도 안 변한다. 입력은 내용이 왼쪽 정렬로
  흐르는 요소라 시작점이 흔들리면 폼 전체가 어긋나기 때문. 버튼과 입력의 결정적 차이.
**라디오·체크 앵커** (2026-08-26 — 킷은 medium·large 둘뿐이라 밀집용 sm 을 한 급 더 얹었다):

| 급 | 동그라미·네모 | 라디오 점 | 체크 표식 | 글자 | 쓰는 자리 |
|---|---|---|---|---|---|
| sm | 18 | 8 | 12 | 14 | 필터 패널 (밀집 UI) |
| md | 20 | 10 | 12 | 15 | 기본 — 로그인 · 약관 동의 |
| lg | 24 | 12 | 16 | 17 | 묶음의 머리 (「전체 동의」) |

- **표식은 동그라미와 짝수 차이**로 준다. 킷이 위치를 `(동그라미 − 표식) ÷ 2` 로 잡아서
  18/9 처럼 홀수 차이면 4.5px 가 되어 점이 반픽셀 밀린다. 18/8 · 20/10 · 24/12 는 전부 짝수다.
- **필터 패널이 sm 을 쓰는 까닭** (2026-08-06 확정) : 킷 medium 은 글자 15 · 원 20 이라,
  폼 라벨(14)·칩(14)·입력(14)만 있는 패널에서 라디오 줄만 혼자 한 급 크게 읽힌다.
  킷 마크업은 못 고치므로 킷이 내건 변수(`--krds-form-check--*-medium`)를 패널 범위에서
  우리 값으로 바꾼다 — 선언 속성을 덮는 게 아니라 변수를 주는 쪽이다.
- 아이콘·글자 간격은 글자크기의 0.4~0.55배 (6~8px).
- 검색창 좌측 아이콘 : 아이콘(18)을 왼쪽 16px 에 두고, 텍스트는 아이콘 뒤 10px 에서 시작 (합계 44).
  입력에 아이콘이 붙는 모든 경우 동일.
- 라운드 : 버튼·검색창은 최대치(캡슐), 입력·셀렉트는 8px. 인터랙티브 명령은 둥글게,
  내용을 담는 상자는 각지게 구분한다.
- 버튼 색 상태 : hover 는 팔레트 한 단계(600→700), active 두 단계(→800). secondary·ghost 처럼
  표면이 흰 것은 hover 에 틴트 표면(cobalt-50 / slate-100), active 한 단계 진하게.
  disabled 는 색 유지 + opacity 0.5.
- **호버는 갈래마다 하나씩만 정한다 (2026-08-07 확정). 자리마다 따로 주지 않는다.**

  | 갈래 | 해당하는 것 | 쉴 때 | hover |
  |---|---|---|---|
  | 면을 가진 조작 | 버튼 primary·secondary · 카드 · 표면형 페이저 | 채움 600 / 흰 면 | 채움 700 · 흰 면은 틴트 50 |
  | **면이 없는 글자 조작** | 버튼 text · 구획 토글 · 패널 링크 · 브레드크럼 | gray 글자 | **cobalt 600 글자. 면을 깔지 않는다** |
  | 칩 | 선택 상태를 갖는 것 | 흰 면 + slate 보더 | 미선택 틴트 50 + 보더 300 / 선택 틴트 100 |
  | **토글 버튼** ※ | 선택 상태를 갖는 **버튼** (학습데이터 검색의 조건 — 재난유형·계절·월) | 꺼짐 흰 면 + 보더 300 / **켜짐 틴트 50 + 보더 500 + 글자 600** | 꺼짐 틴트 50 + 보더 300 / 켜짐 틴트 100 + 보더 600 |

  ※ **토글 버튼은 아직 학습데이터 검색 한 화면에서만 쓴다 (2026-08-07).** 다른 화면의 조건 줄은
  칩 그대로 두고, 넓힐지는 그 화면을 잡을 때 다시 본다 — 한 화면에서만 검증된 갈래다.

  글자 조작에 면을 깔면 버튼과 같은 갈래로 읽힌다. **면이 없다는 것이 그 조작의 무게**이므로
  반응도 글자에서 끝낸다. 한 줄에 text 버튼과 secondary 버튼이 같이 서는 자리(필터 패널
  버튼 줄)에서 둘 다 면이 생기면 무게 차가 사라진다.

  **토글 버튼의 켜짐은 채움이 아니라 틴트다 (2026-08-07 확정).** 칩과 같은 값을 쓴다 — 모양은
  버튼(캡슐·버튼 여백)이고 색만 칩 사다리를 따른다. 꺼짐 보더는 입력과 같은 300 이다 (킷 버튼
  기본 600 은 스물 넘는 조건이 늘어서면 낱개마다 진한 테두리가 그어져 줄이 격자로 읽힌다). 채움(primary)을 안 쓰는 이유는 한 패널에서
  **채움이 실행 하나의 무게**이기 때문이다: 조건 토글이 같은 채움을 쓰면 여덟·열둘이 켜지는 줄에서
  코발트 덩어리가 여럿 생겨 검색 버튼이 묻힌다. 꺼짐 hover(틴트 50)와 켜짐(틴트 50)은 면이 같으므로
  **보더가 둘을 가른다**(300 vs 500). 명도차가 작은 갈래라 `aria-pressed` 를 함께 둔다.
  링크(`variant="link"`)는 쉴 때부터 파란 글자라 이 갈래가 아니다 — hover 는 한 단계 더(700).
  킷 text variant 는 hover 에 `secondary-5` **면**을 까는 것이 기본값이라 이 규칙과 어긋난다.
  테마에서 면을 끄고 글자색으로 옮긴다 (krds-theme.css § 버튼).

정사각 아이콘 버튼 (페이저 화살표·카드 액션·패널 토글):

| | 박스 | 아이콘 | 비율 |
|---|---|---|---|
| sm | 28 | 16 | 0.57 |
| md | 32 | 18 | 0.56 |
| lg | 36 | 18 | 0.50 |

- **아이콘 ≈ 박스의 절반** (0.5~0.57). 라운드는 8px (캡슐 아님 — 버튼과 달리 보조 조작이라 각을 남긴다).
- 페이지네이션 숫자 셀은 36 정사각 + 라운드 8, 셀 간격 6.


### 급을 고르는 규칙 (2026-08-26 확정)

위 앵커가 "각 급이 몇 px 인가" 라면, 이건 **어느 자리에 어느 급을 쓰는가** 다. PC·모바일 공통이다.

| 급 | 쓰는 자리 | 한 화면에 |
|---|---|---|
| **lg** | 그 화면을 **끝내는** 제출 버튼 — 누르면 화면이 넘어간다 (로그인 · 다음 · 등록 · 확인) | **하나만** |
| **md** | 구획(블록) 안의 액션 — 그 구획 안에서만 작동한다 (검색 · 다운로드 신청 · 영상 생성) | 구획마다 하나씩 |
| **sm** | 목록·표 줄 안, 그리고 여럿이 나란히 붙는 자리 (필터 칩 · 저장된 조건 · 초기화) | 여러 개 |

- **급은 크기, 채움/흰 면은 무게 — 둘은 따로 정한다.** 한 구획 안에 **채움은 하나**뿐이다.
- 모바일에서 **lg 제출 버튼은 가로 전체를 채운다.**
- 예외로 남겨 둔 자리 : 학습데이터 상세의 액션 줄(즐겨찾기·다운로드 sm + 다운로드 신청·저작도구로
  열기 md)은 위계가 다르다고 보아 두 급을 섞은 채로 **유지한다** (2026-08-26 판단).

### 모바일 규칙 (767 이하 · 2026-08-26 확정)

**모바일용 크기를 따로 만들지 않는다.** 값은 위 앵커의 36 / 44 / 52 **한 벌뿐**이고,
모바일에서는 **한 급 위를 쓴다.**

| | 모바일에서 |
|---|---|
| 컨트롤 **sm 자리** | **md 를 쓴다** — 높이 36→44 · 글자 14→15 · 좌우 여백 14→20 |
| 컨트롤 **md · lg** | **그대로** (44 · 52) |
| 라디오·체크 **sm 자리** | **md 를 쓴다** — 동그라미 18→20 · 점 8→10 · 글자 14→15 |
| 라디오·체크 **md · lg** | **그대로** (20 · 24) |

- **왜 sm 만인가** : 손가락 최소 크기(44)를 밑도는 것은 **sm(36) 하나뿐**이다. md·lg 는 원래 넘는다.
  세 급을 다 올리면 PC 에 없는 외톨이 숫자(60)가 생기고 지켜야 할 표가 두 벌이 된다
  (한때 44/52/60 으로 두었다가 2026-08-26 되돌렸다).
- **대가** : 모바일에서 sm 과 md 가 같은 크기(44)가 된다. **세 단이 두 단이 된다** —
  필터 칩과 검색 버튼의 키가 같아진다. 위계는 크기가 아니라 **채움/흰 면**이 진다
  (위 § 급을 고르는 규칙 — "급은 크기, 채움은 무게").
- **lg 제출 버튼은 가로 전체를 채운다.** 폼 아래 오른쪽에 작게 붙어 있으면 끝이 안 보인다.

정사각 아이콘 버튼:

| | 박스 | 아이콘 |
|---|---|---|
| PC | 28 / 32 / 36 | 14 / 16 / 18 |
| **모바일** | **44 (컨트롤 md) — 세 급 모두** | **16 / 18 / 20** (§8 아이콘 사다리 한 칸 위) |

- 박스는 배경이 없어 눈에 안 보이는 자리다. 세 급을 다 44 로 두고 **아이콘 크기로만 급을 가른다.**
- 킷이 직접 그리는 아이콘 버튼(모달 닫기 ✕ · 비밀번호 보기 눈 · 검색어 지우기)은
  **박스만 44 로 넓히고 글리프는 킷 값(24) 그대로다** — 보이는 크기는 안 바뀐다.

그 밖의 누르는 것 — 전부 **md(44)** 로 떨어진다. 새 숫자가 없다:

| | PC | 모바일 |
|---|---|---|
| 페이지네이션 숫자 셀 | 36 (sm) | **44 (md)** |
| 면이 없는 글자 조작 (정렬 드롭다운 · 초기화 · 로그인) | 글자 높이 (23~27) | 누르는 자리 **44** — **글자 크기·모양은 그대로** |
| 라디오·체크 **누르는 줄** | 글자 높이 (26~33) | 줄 키 **44** — 누르는 자리는 이름표 전체다. 동그라미·글자는 그대로고 위아래 빈자리만 늘어난다 |
| 푸터 링크 (행정안전부 · 정부24 등) | 25 | **그대로** — 정보성 링크다. 44 를 주면 푸터가 두 배로 길어진다 |

- **왜 이 규칙이 필요한가** : KRDS 킷은 767 이하에서 컨트롤 글자를 자기 모바일 값(15/17/19)으로
  갈아 끼우는데 **상자 높이는 그대로 둔다.** PC 자리만 채워 두면 모바일에서 글자만 커져 상자가 꽉 차고,
  그때그때 손댄 자리와 안 댄 자리가 섞여 **같은 화면 안에서 급이 갈린다** (2026-08-26 실측으로 확인).
  **라디오·체크도 같은 사정이었다** — 로그인 「아이디 저장」이 PC 15 인데 휴대폰에서 17 로,
  회원가입 동의줄이 15→17 · 17→19 로 커졌다. 동그라미는 그대로여서 줄이 어긋나 보였다.
- **왜 전역 타이포로 한 번에 못 막는가** : 위 §2 전역 스케일이 **PC 트랙만 재정의**하기로 되어 있다
  (모바일 트랙 불변). 그래서 컴포넌트마다 `mobile-` 자리를 따로 채워야 하고, 사다리가 그 일을 대신한다.

### ★ 이 규칙을 코드에서 읽는 법 — 「컨트롤 사다리」 (2026-08-26)

**크기를 자리마다 적지 않는다.** 위 값들은 krds-theme.css 맨 앞 § 컨트롤 사다리 에
토큰 한 벌로 들어가 있다. 버튼·입력·셀렉트·검색창·드롭다운·아이콘 버튼·페이저·
카카오 인증 버튼이 전부 이 토큰을 가리킨다.

| 무엇을 정할 때 | 읽을 토큰 |
|---|---|
| 컨트롤 높이 | `--klid-control-height-sm` / `-md` / `-lg` |
| 컨트롤 글자 | `--klid-control-font-sm` / `-md` / `-lg` |
| 버튼 좌우 여백 | `--klid-control-padding-sm` / `-md` / `-lg` |
| 정사각 아이콘 버튼 | `--klid-iconbtn-box-*` · `--klid-iconbtn-glyph-*` |
| 라디오·체크 | `--klid-check-box-*` · `--klid-check-dot-*` · `--klid-check-mark-*` |
| 면이 없는 조작의 누르는 자리 | `min-height: var(--klid-tap-min)` |
| 좁혀 둔 자리의 누르는 자리 | `max(제값, var(--klid-tap-floor))` |

- **모바일 분기는 사다리 안에 한 개뿐이고, 거기서도 새 값을 적지 않는다** —
  `--klid-control-height-sm: var(--klid-control-height-md)` 처럼 **md 를 가리키기만 한다.**
- **새 화면·새 부품은 급(sm·md·lg)만 고른다.** 그러면 모바일까지 저절로 맞으므로
  **화면 CSS 에 767 블록을 두지 않는다.**
- 사다리 밖에 두는 것 : 크기가 의미를 갖는 **그림** (로고 마크 36 · 영상 재생 표식 44).
  누르는 물건이 아니라서 폭에 따라 커질 이유가 없다.
- 킷이 `height` 를 직접 거는 자리(드롭다운 트리거 등)도 사다리를 읽게 해 뒀다 — 화면에서
  다시 조준하지 않는다. 종전에는 화면마다 그 조준을 다시 했다 (생성형 AI·학습데이터 상세).

---

## 4. 칩 · 배지 (텍스트 캡슐류)

실측 앵커:

| | 가로 패딩 | 세로 패딩 | 글자 | 실효 높이 | 라운드 |
|---|---|---|---|---|---|
| 정보 배지 (상태·이벤트) | 10 | 2 | 13·500 | 24 | 캡슐 |
| 사각 태그 (출처) | 6 | 2 | 13·500 | 24 | 6 |
| 필터 칩 xs | 8 | 3 | 13·500 | 26 | 캡슐 |
| 필터 칩 sm | 10 | 4 | 14·500 | 30 | 캡슐 |
| 필터 칩 md | 14 | 6 | 14·600 | 34 | 캡슐 |
| 선택 요약 칩 (× 달림) | 좌 10 · 우 8 | 4 | 14·400 | 30 | 캡슐 |

비례 규칙:

- **가로:세로 패딩 비율이 역할을 말한다** : 읽기만 하는 정보 배지는 5:1 (납작하고 가로로 긴 알약),
  클릭하는 칩은 2.3~2.5:1 (통통해서 누를 수 있어 보임). 사각 태그는 3:1 + 라운드 6 으로
  가장 조용하게 — 다만 **굵기는 캡슐과 같다** (2026-08-25 수정 · 종전 500). 조용함은 색과
  모양이 지고, 굵기까지 낮추면 한 줄에 나란히 설 때 혼자 잘못 찍힌 것처럼 보인다.
  **배지 굵기는 500 이다** (2026-08-25 확정 · 종전 600) — 색이 면으로 넘어가 글자가 검정
  하나가 된 뒤로 600 은 배지가 같은 줄의 제목·값과 무게를 다툰다. 배지는 읽는 자리가 아니라
  훑는 표식이라 물러나야 한다. 다만 400 까지 내리면 표에서 본문 글자와 구분이 사라진다 —
  500 은 본문(400) 한 단 위, 강조(600) 한 단 아래다.
- 칩 md(34)는 버튼 sm(36)과 같은 줄에 놓아도 어긋나지 않는 높이다. 필터바에서 칩과 버튼을
  섞을 때 이 정렬을 전제로 한다.
- 칩 크기는 위계다 : 핵심 필터 줄 md, 보조 조건 줄 sm. 역할이 같으면 색은 같고 크기만 바뀐다.
  단, **조건이 여섯을 넘는 필터 패널은 한 급씩 내린다** (2026-08-07 확정): 펼쳐 둔 조건 sm,
  접어 둔 조건(상세 검색 안) xs. md 는 쓰지 않는다 — 칩이 스물을 넘어가면 md 는 패널 세로만 늘린다.
  **접힘/펼침이 곧 위계**이므로 크기 차이가 그 위계를 되풀이해 준다.
- **xs(26)는 접어 둔 조건 줄 전용** (2026-08-07 확정). 한 조건에 칩이 열둘씩 붙는 자리(월)가
  sm 으로는 줄이 접힌다. xs 는 글자를 13 으로 내리므로 **아이콘도 14 로 함께 내린다** —
  글자보다 크면 칩 안에서 아이콘이 먼저 읽힌다. 킷은 small 이 최하라 킷 변수
  (`--krds-form-chip--*-small`)를 덮어 만든다.
  버튼 sm(36)과 같은 줄에 놓지 않는다: 10px 차이라 줄이 어긋나 보인다.
- **선택 요약 칩** (2026-08-06 확정) : 지금 걸린 조건을 패널 맨 아래 한 줄로 모으는 자리.
  조건이 여러 줄에 흩어지거나 일부가 접혀 있으면 무엇을 골랐는지 훑을 곳이 없어진다.
  치수는 필터 칩 sm 을 따르되 오른쪽 패딩만 8 (× 14px 은 글자보다 시각 무게가 가벼워
  좌우를 같이 주면 오른쪽이 더 벌어져 보인다). 색은 아래 틴트 공식 그대로라 위에서 켜 둔
  칩과 같은 계열로 읽힌다. 칩 사이 간격은 8 — 고르는 칸이 아니라 목록이라 12 보다 붙는다.
  걸린 조건이 없으면 줄을 통째로 내지 않는다.
- 배지 안 아이콘은 14px (글자 13 대비 1.05배 — 글자와 거의 같은 크기), 간격 4.
- **한 카드에 배지가 여럿 서면 역할마다 모양을 준다** (2026-08-07 확정). 색만 바꾸면 색이
  셋·넷이 되어 무엇이 갈래이고 무엇이 수치인지 오히려 안 읽힌다. 학습데이터 카드가 그 자리다.
  **배지는 전부 킷 `Badge` 로 그린다. 우리가 얹는 것은 색과 모양뿐이다** (2026-08-25 정리 —
  종전에는 넷이 다 커스텀 부품이었고 같은 배지 줄이 세 화면에 복사돼 있었다). 부품으로 남은 것은
  `EventBadge` 하나인데, 그것도 모양 때문이 아니라 **유형 코드에서 글리프와 이름을 꺼내 짝짓는
  조립** 때문이다. 파일 종류·수치는 배지를 쓰지 않는다:

  | 순서 | 변형 | 역할 | 무엇 | 모양 | 색 |
  |---|---|---|---|---|---|
  | 1 | `.klid-badge-event` | 분류 | 이게 무엇인가 (재난유형) | 캡슐 · 흰 면 + gray-30 선 | **아이콘에만** 유형색, 글자 gray-90 |
  | 2 | `.klid-badge-flat` | 구성 | 무엇이 붙어 있나 (증강) | 캡슐 · 회색 면 | 글자 **코발트** |
  | 3 | `.klid-badge-flat` + `.klid-badge-square` | 출처 | 어디서 왔나 (내부·외부) | 각진 태그 · 회색 면 | 글자 내부 **초록** · 외부 **무채** |
  | — | — | 종류 | 무슨 파일인가 (이미지·영상·데이터셋) | **배지 없이 글리프** (`KindLabel`) | 무채 |
  | — | — | 수치 | 얼마인가 (다운로드·건수) | 배지 없이 글자만 | gray-60 · `tabular-nums` |

  **아이콘이 붙는 갈래는 분류 하나다** (2026-08-25 확정) — 유형 글리프를 가진 것이
  재난유형뿐이라서다. 나머지 둘은 색으로 갈린다.
  **증강은 코발트다** (2026-08-25 확정 · 종전 violet). 보라는 재난유형 「유괴」가 이미 쓰는 색이라,
  유괴 데이터에 증강이 붙으면 한 줄에 같은 보라가 두 뜻이 됐다. 남은 색이 다 임자가 있어 새 색을
  들이는 대신 **자리로 가른다** — 코발트는 증강(갈래)과 시행중(상태), 초록은 내부와 완료,
  노랑은 외부와 진행중이 나눠 쓴다. 갈래 배지와 상태 배지가 한자리에 서는 화면은 없다.
  `source` 의 외부는 종전 information 파랑이었다 (2026-08-25 노랑으로 변경) — 파랑은 링크·버튼·
  글자 분류 배지가 이미 쓰는 색이라 태그에서 한 번 더 쓰면 무뎌졌다.

  **카드에 서는 순서가 곧 훑는 순서다** — 무엇인지 → 무엇이 붙었는지 → 어디서 왔는지.
  분류 배지가 **필터의 꺼진 칩과 같은 모양**인 것이 핵심이다: 필터에서 고르던 모양이 결과에
  그대로 있어야 "내가 고른 그것"이 이어진다. 분류 배경까지 유형색으로 물들이지 않는다.
  캡슐 둘(분류·구성)은 색으로, 사각 태그(출처)는 모양으로 갈린다 — 셋 다 캡슐이면 색으로만
  갈려야 하는데 그러면 한 줄에 색이 셋이 되어 무엇이 갈래인지 흐려진다.
- 선택 상태의 강도 사다리 : 다중선택은 틴트(배경 cobalt-50 + 보더 500 + 글자 700),
  단일선택은 채움(배경 600 + 흰 글자). 회색 캔버스 위에서 미선택(흰 배경)보다 반드시 강해야 한다.
- **선택지에 "전체"가 섞인 단일선택은 칩이 아니라 라디오** (2026-08-06 확정). 붙인 세그먼트는
  갈래 몇 개가 **동등하다**고 말하는 모양이라, "전체"(= 조건을 안 거는 상태)가 나머지와 같은 값처럼
  읽힌다. 라디오는 목록 중 하나를 고르는 모양이라 "전체"가 기본값 자리에 자연스럽게 앉는다.
  필터의 데이터 유형·증강 포함 여부가 그 경우다. 세그먼트는 "전체"가 없는 진짜 갈래에만 쓴다.
- **칩 줄 간격은 12** (2026-07-30 확정). §6 사다리의 "한 컴포넌트 안"(10~16) 구간이다. 킷은 그룹
  컨테이너에 20 을 쓰는데 그건 세로로 쌓인 라디오·체크박스 목록 기준값이라, 가로 칩 줄에 그대로
  두면 칩끼리 남처럼 떨어진다. 세로 목록은 킷 기본(20) 유지.
- **체크(✓) 표식은 다중선택만** (2026-07-30 확정). 여러 개가 켜질 수 있어 어느 게 켜졌는지 훑어야
  하는 다중선택은 유지하고, **단일선택은 끈다**(`.klid-chip-no-check`) — 하나만 채워지므로 표식이
  겹치고, 선택 칩만 폭이 넓어져 줄이 고르지 않다. 색만으로 구분하는 게 아니라(채움 vs 흰 배경의
  명도 차) 표식을 꺼도 WCAG 1.4.1 위반이 아니다.
- 틴트 색 쌍 공식 : **배경은 팔레트 50, 글자는 같은 팔레트 600~700** (예: green-50 + green-700).
  모든 상태 배지가 이 한 공식으로 만들어진다.

### 상태 배지 색 (2026-08-25 확정 — 정본)

**뜻이 색을 정한다. 화면이 고르지 않는다.** 종전에는 이 표가 없어 화면마다 각자 골랐고,
같은 말이 화면을 옮기면 다른 색으로 떴다 (「만료」가 학습데이터 상세에선 빨강 · 내 학습데이터에선
회색, 「기한임박」이 주황과 빨강, 「시행중」이 **한 화면 안에서** 파랑과 초록).

| 뜻 | 색 | 이 말들이 여기 온다 |
|---|---|---|
| 지금 손대야 하는 것 | **danger 빨강** | 기한임박 · 중요 |
| 아직 안 끝난 것 (기다리는 중) | **warning 주황** | 진행중 · 대기 · 처리중 · 접수 |
| 쓸 수 있는 것 · 끝맺은 것 | **success 초록** | 다운로드 가능 · 완료 · 답변완료 |
| 살아 있는 것 (지금 이것이 유효) | **primary 파랑** | 시행중 |
| 끝난 것 (더는 손댈 게 없음) | **gray 회색** | 만료 · 폐지 · 중단 · 취소 · 실패 |

- **빨강은 "급하다"에만 쓴다.** 만료는 이미 끝난 일이라 급할 것이 없다 — 회색으로 물러난다.
  빨강을 끝난 것에도 주면 정작 지금 손대야 하는 줄이 안 튄다.
- **주황과 파랑을 헷갈리지 않는다.** 주황은 *아직* 안 끝난 것, 파랑은 *지금* 유효한 것이다.
- **면은 옅은 회색 하나(gray-5)이고 색은 글자에만 든다** (2026-08-25 확정 · `.klid-badge-flat`).
  **상태·갈래 배지 모두** 이 규칙을 따른다 — 색 면을 쓰는 배지가 하나라도 남으면 그것만 튀고,
  한 화면에 색 면이 대여섯이 되면 무엇이 갈래인지 색으로는 안 갈린다. 면이 다 같아지면 갈래를
  말하는 것은 **모양**(캡슐 · 각진 태그 · 흰 면 분류)과 글자색뿐이다.
  위 틴트 공식은 분류 배지의 흰 면 + gray-30 선에만 남는다.
- **외부는 무채다** (2026-08-25 확정 · 종전 warning 노랑). 회색 면 위에서 노랑 글자는 갈색으로
  가라앉는 데다, 남은 색(빨강은 위험 · 코발트는 증강)이 다 임자가 있다. 내부(초록)만 색을 갖고
  **밖에서 온 것은 조용히 두는 것**이 뜻과도 맞는다 — 이 포털이 만드는 것은 내부 학습데이터다.
- **글자는 팔레트 50이다** (같은 날 확정). 색을 지는 것이 글자 하나뿐이라 60은 탁하게 보인다.
  다만 대비 하한(4.5:1, 면 gray-5)이 우선이라 **올릴 수 있는 것만 올린다** —
  빨강 4.62 · 초록 5.24 · 코발트 6.69 는 50, **노랑은 50이 3.04로 미달이라 60**(4.80)을 지킨다.
  회색(만료·중단)은 물러나는 것이 뜻이라 올리지 않는다.

### 글자로만 말하는 분류 배지 (2026-08-25 확정)

상태가 아니라 **갈래**를 말하는데 그림이 없는 것 — FAQ 분류, AI 모델 파일 종류.
**파란 선 캡슐**(`variant="outline" color="primary"`)로 통일한다. 상태 배지가 틴트라 선만으로
갈리고, 색을 하나만 쓰므로 갈래가 늘어도 색이 불어나지 않는다.

### 파일 종류는 배지가 아니다 (2026-08-25 확정)

이미지 · 영상 · 데이터셋 셋은 **글리프로 말한다** (`KindLabel`). 잠깐 배지(초록 선 이미지 ·
보라 선 영상)로 그렸다가 걷어냈다 — 이 값이 서는 자리는 이미 같은 줄에 배지를 갖고 있어
(표에는 상태 배지, 상세 머리에는 분류·구성·출처) 알약이 한 줄에 둘씩 서면 어느 쪽이 상태이고
어느 쪽이 종류인지 모양으로 안 갈렸다.

- 글리프는 **무채**다 — 색 있는 글리프는 재난유형 하나뿐이라 그것이 곧 유형이라는 신호다.
- 같은 낱말이 줄마다 되풀이되는 표에서는 이름을 떼고 글리프만 세우되, 표 위에 범례를 한 번 세운다.

### 남은 날(D-day)에는 색을 얹지 않는다 (2026-08-25 확정)

며칠부터 임박으로 볼지가 아직 안 정해져 있다. 기준 없이 붉게 칠하면 색이 아무것도 안 가리킨다.
급한 건이 있다는 사실은 **배지가 글자로** 말한다 (「기한임박 N」). 기준이 정해지면 되살린다.

---

## 5. 표면 (카드 · 표 · 모달 · 아코디언)

공통: 표면 흰색 + 보더 slate-200 (1px) + 라운드 16. 그림자는 기본 없음 — 강조 카드만
정적 소프트 섀도(card-soft), 떠 있는 레이어(팝업·모달)만 shadow-lg/2xl.

| | 값 |
|---|---|
| 카드 패딩 | 20 (밀집 목록·사이드) / 24 (기본 본문) 두 단계만 |
| 카드 썸네일 높이 | 160 (목록) / 120 (추천·연관 컴팩트) 두 단계만 |
| 표 셀 | 가로 12 · 세로 12 (헤더도 세로 12) |
| 표 끝 열 | 20 (카드 패딩과 정렬 — 셸에 딱 붙지 않게) |
| 표 행 실효 높이 | ≈44 (글자 15 × 1.6 + 상하 12) |
| 표 구분선 | 헤더 밑 slate-200 · 행 사이 slate-100 |
| 모달 | 최대폭 512 · 헤더 24/16 · 본문 24/20 · X 버튼 32(아이콘 20) |
| 아코디언 | 헤더 세로 16 · 패널 상단선 slate-200 + 위 16 · 항목 간격 10 · **헤더·패널 모두 흰 표면** |
| 아코디언 Q·A 표식 | 28 정사각 · 라운드 8 · 글자 14·600 · Q = 코발트 틴트(primary-5 / primary-60) · A = 회색(gray-20 / **gray-60**) |
| 빈 상태 | 본문 py 80 / 카드 안 py 56 / 표 안 py 24 · 표면 slate-50 |
| 드롭존 | py 40 (모달·본문) / 32 (패널) · 2px 점선 slate-300 |

비례 규칙:

- **패딩 ≥ 라운드 × 1.25** (라운드 16 → 패딩 20~24). 큰 패널(라운드 24)은 패딩 32 급.
- **상세 화면의 구획은 공통 구획 카드 하나로 묶는다** (2026-08-08 확정). 흰 면 + gray-20 보더 +
  라운드 16 + 패딩 24 — 이 표의 값 그대로다. 클래스는 `.klid-section-card`(krds-theme.css)
  **하나만 쓰고 화면마다 새로 만들지 않는다.** 학습데이터 상세와 AI 모델 상세가 같은 선언을
  이름만 달리해(`.klid-ds-card` · `.klid-aim-card`) 각자 갖고 있었는데, 이름이 다르면 중복으로
  보이지도 않아 이 표의 값이 바뀔 때 고칠 곳을 놓친다. 같은 이유로 **화면을 넘나드는 부품
  (다운로드 신청 모달의 `.klid-dl-*`)도 공통층이 갖는다** — 클래스 이름이 같은데 선언이 두
  화면에 갈려 있으면 SPA 에서 나중에 로드된 쪽이 이겨, 어느 화면을 먼저 들렀는지에 따라 모양이
  달라진다. 실제로 약관 박스가 그랬다(16rem/패딩 20 vs 12rem/패딩 16).
- **카드 안 대표 이미지는 가장자리까지 꽉 채운다** (2026-08-03 확정). 패딩을 카드가 아니라
  본문 영역이 갖고, 이미지 모서리는 카드가 `overflow: hidden` 으로 잘라낸다. 이미지에 따로
  라운드를 주지 않는다 — 카드 라운드를 그대로 따르므로 §7("안쪽이 바깥보다 작다")이 저절로 지켜진다.
  패딩 안에 가두면 그림이 작아져 답답하고, 목록 카드는 그림이 먼저 읽혀야 한다.
- **추천·연관 카드는 썸네일만 120 으로 낮춘다** (2026-08-05 확정). 그 화면의 본 목록이 아닌데도
  같은 160 을 쓰면 카드 넉 장이 화면 한 판을 차지해, 보조 정보가 본문보다 무거워진다. 글자 크기와
  패딩은 건드리지 않는다 — 같은 종류의 카드로 읽혀야 하므로 낮추는 것은 그림 하나뿐이다.
- **조건이 많은 필터 패널은 행형(rows)** (2026-08-06 확정). 한 줄(위아래 패딩 14 + 아래 구분선.
  **패널 안의 모든 구분선이 같은 14/14 를 쓴다** — 실행 줄 위 선까지. 2026-08-07 통일)에
  조건을 여럿 놓고, 라벨은 컨트롤 **위**에 얹는다 — 라벨을 왼쪽 칸에 두면 라벨 길이 편차가 그대로
  라벨→컨트롤 간격 편차가 되어 줄마다 사이가 들쭉날쭉해 보인다. 조건끼리는 48 로 벌린다
  (라벨→컨트롤 밀착 8 의 여섯 배라 "이 라벨은 이 컨트롤의 것"이 먼저 읽힌다).
  **칸 폭은 내용이 정하고 남는 폭은 한 칸만 먹는다** — 줄마다 조건 수가 다를 수 있어(둘·셋)
  모든 줄이 공유하는 세로 정렬선은 성립하지 않는다. 공유하는 선은 왼쪽 시작선 하나다.
  짝으로 읽혀야 하는 조건(시/도 ↔ 시/군/구)만 폭을 같게 못 박는다.
- 표 셀 간 여백은 인접 패딩 합 24 — 컬럼 간격(§1 의 24)과 같은 값이라 표가 그리드와 한 리듬이 된다.
- 모달 헤더는 본문보다 세로가 얇다 (16 vs 20). 제목 줄은 조이고 내용은 풀어준다.
- **창 크기에 따라 제목·걸음이 한 급 갈린다** (2026-08-10 확정).

  | | 폭 | 제목 | 걸음 |
  |---|---|---|---|
  | 작은 창 (sm) | 400 | **20** | **md 44** |
  | 폼 모달 (md) | 512 | 24 | lg 52 |

  킷은 크기와 무관하게 제목 24 · 버튼 52 다. 400 폭에 글이 두어 줄뿐인 창(세션 만료·로그아웃·
  저장된 검색 조건·안내 창)에서는 제목이 위를 누르고 52 버튼 둘이 아랫동을 버튼 덩어리로 만든다.
  폼 모달은 세로가 600 을 넘고 입력이 여럿이라 제목이 머리를 잡고 결정 버튼이 끝맺음을 보여야
  하므로 킷 값 그대로다. 제목은 공통층(`.modal-dialog.modal-sm` 토큰)이, 걸음은 각 창의
  `size` prop 이 갖는다 — 버튼 크기는 CSS 가 아니라 컴포넌트 API 이기 때문이다.
- 구분선 위계 : 바깥 경계(slate-200) > 내부 리스트(slate-100) = 표 행(slate-100).
  안쪽으로 들어갈수록 옅어진다.
- **표 머리 줄은 면을 갖는다** (2026-08-24 확정) : 면 slate-50 · 글자 13 · 굵기 400 · 색 slate-600.
  표가 시작되는 자리를 면이 알리고, 값보다 뒤로 물러나는 일은 **크기와 굵기**가 맡는다.
  ★ 색을 더 연하게 두지 못한다 — 면이 조금이라도 들어가면 slate-500 은 4.40:1 로 §8 하한에
    미달한다(흰 바탕에서만 4.76:1 로 겨우 통과하던 값이다). 종전 규칙(14 유지 · slate-400 ·
    500)은 머리 줄이 흰 면이던 시절의 값이다.
- **목록 표의 줄은 마우스가 얹히면 primary-5 로 짚는다** (2026-08-24 확정). 값이 여럿인 줄을
  가로로 읽는 동안 눈이 줄을 놓치지 않게 하는 장치라, 셸을 가진 목록 표(`.klid-table-shell`)에만
  준다 — 두 칸짜리 정보 표에는 짚을 줄이 없다.
- **줄무늬(zebra)는 쓰지 않는다** (2026-08-24 확정). 한때 홀수 줄에 옅은 하늘색을 깔아 봤으나
  걷어냈다 — 이 프로젝트의 표는 줄마다 상태 배지·붉은 D-day 같은 **값 쪽 색**을 갖고 있어,
  면에까지 색을 쓰면 표 안에 색이 둘이 된다. 줄을 가르는 일은 구분선이 맡는다.
- **아코디언은 펼쳐도 배경을 바꾸지 않는다** (2026-07-30 확정). 질문/답변 구분은 A 표식과 패널
  상단 구분선이 맡는다. 예전에는 열림 패널을 slate-50 으로 눌렀는데, 흰색과 명도차가 1~2% 뿐이라
  "한 톤 눌렀다"는 의도가 실제로는 읽히지 않았다.
- **틴트 표면 위 글자는 gray-50 을 쓰지 않는다.** gray-50 on gray-20 은 3.86:1 로 미달이라
  gray-60(6.15:1)부터 쓴다. 배지 disabled · 아코디언 A 표식이 같은 자리였다. 특히 CSS `::before`
  로 그린 글자는 대비 감사 스크립트가 훑지 못하므로 값을 넣을 때 직접 확인한다.

---

## 6. 페이지 세로 리듬

| 간격 | 값 | 쓰임 |
|---|---|---|
| 블록 사이 | 20 | 헤더 다음 첫 요소, 필터 다음 표 |
| 구획 사이 | 32 | 성격이 다른 구획으로 넘어갈 때 |
| 그룹 사이 | 48 | 페이지 안 큰 단락 |
| 섹션 상하 | 80 | 메인·소개 페이지 섹션 (히어로는 96~128) |

- 비율로 1 : 1.6 : 2.4. 이 3단 밖의 세로 간격(24, 28 등)을 같은 자리에 섞지 않는다.
- **헤더 다음 첫 요소가 덩어리진 구획이면 20 대신 32** (필터바·탭줄·검색 카드 등, 2026-07-30 확정).
  20 은 가벼운 요소가 바로 오는 경우의 값이라, 면적을 가진 구획이 붙으면 제목에 눌러붙어 보인다.
- **리듬 규칙(`> * + *`)에서 모달을 뺀다** (2026-08-10 확정). 창은 대개 화면 컨테이너의 **직계
  자식**으로 놓이는데(딤까지 한 요소다), 리듬이 그것까지 밀면 `position: fixed; inset: 0` 이라도
  `margin-top` 만큼 내려앉아 **같은 창이 화면마다 다른 높이에 뜬다** (AI 모델 상세에서 48 어긋났다).
  선택자에 `:not(.krds-modal)` 을 적는다 — 공통층에서 `.krds-modal { margin: 0 }` 으로 막는 방법은
  페이지 규칙(0-2-0)이 더 세서 진다. 리듬은 **본문 구획끼리만**이라는 뜻을 선택자에 적는 쪽이 맞다.
- **킷이 자기 여백을 갖는 컴포넌트는 그 값을 존중한다** (2026-07-30 확정). 예: 페이지네이션 위 여백은
  KRDS 규격 40(`--krds-pc-gap-layout-h2-h3`)이며 3단 사다리로 덮지 않는다. KRDS 가 정한 레이아웃
  리듬이라 우리 값으로 바꾸면 같은 토큰을 쓰는 다른 자리와 어긋난다.
- **브레드크럼은 현재 페이지까지 표시한다** (2026-07-29 확정: 일반 웹 관행·WAI 패턴 채택.
  KRDS 패턴은 상위 경로만 쓰지만 위치 파악 편의를 우선). 마지막 항목은 비링크
  (킷 Breadcrumb 에 disabled: true — aria-current 는 킷이 처리). 타이틀(h1)은 별도 유지.
  - **단, 상세 화면은 그 상세 항목을 넣지 않는다** (2026-08-07 확정). 목록·폼 화면은 위 규칙
    그대로이고, 개별 건을 여는 상세(학습데이터 상세·AI 모델 상세·워크스페이스 상세 등)에서만
    마지막 한 칸을 뺀다. 상세 항목의 이름은 **바로 아래 제목(h1)이 이미 말하는 같은 말**이고,
    `화재 감지 모델 v2.1` 처럼 길어서 경로 줄을 혼자 다 먹는다. 이때 마지막에 남는 목록 항목은
    **비링크로 바꾸지 않는다** — 현재 페이지가 아니라 돌아갈 곳이라 눌려야 한다.
- **페이지 헤더는 한 줄** : 타이틀(h1) 좌 + 브레드크럼 우, 베이스라인 정렬 (.klid-page-head, 2026-07-29).
- **결과 머리는 위 32 · 아래 12** (2026-08-07 확정). 목록을 늘어놓는 화면에서 건수(왼쪽)와
  정렬(오른쪽)이 서는 한 줄이다. 위는 앞 구획(필터 패널·리드문)에서 넘어오는 전환 32 이고,
  아래 목록과는 12 로 붙인다 — **건수·정렬은 그 목록을 설명하는 줄이지 별개 구획이 아니다.**
  둘 사이를 같은 32 로 벌리면 머리가 위 구획에 붙어 목록과 갈라져 보인다. 값은 화면마다 두지
  않고 공통 층이 갖는다 (`.klid-result-head` — krds-theme.css). 정렬 컨트롤은 커스텀
  `Dropdown variant="sorting"` 이다 (킷 `Select` 는 펼친 목록을 OS 가 그려 화면이 갈린다).
- **표면 없이 맨바닥에 서는 줄은 좌우로 8 들여쓴다** (2026-08-10 확정). 목록 화면에는 흰 면을
  가진 것(필터 패널·카드·표 셸)과 배경 위에 글자만 얹힌 줄(결과 머리·구획 소제목·저장분 줄 등)이
  번갈아 선다. 맨바닥 줄을 컨테이너 끝(0)에 두면 **옆의 면들보다 바깥으로 튀어나온 것처럼
  읽힌다** — 면은 보더 안쪽에서 글이 시작하는데 맨바닥 줄은 0 에서 시작하기 때문이다.
  8 은 그 차이를 메우되 카드 패딩(16~24)까지 따라가지는 않는 값이다: 다 맞추면 이번엔 줄이
  안으로 밀려 들어가 보인다.
  - **면을 가진 것과 그것을 담은 줄에는 주지 않는다.** 카드 그리드·표 셸·아코디언이 그렇다 —
    주면 면의 왼쪽 선이 위아래 면과 어긋난다. 구획이 소제목과 카드를 함께 가지면 **소제목 줄만**
    들여쓴다.
  - **페이지 제목·브레드크럼(`.klid-page-head`)과 리드문도 같이 들어간다** (2026-08-10 확정).
    처음에는 제목을 "화면의 기준선"이라며 예외로 뒀는데, 정작 그 아래 줄이 전부 8 로 들어가니
    제목만 혼자 왼쪽으로 튀어나왔다. **기준선은 이제 8 자리다** — 본문의 맨바닥 글은 제목부터
    구획 소제목까지 한 선에 선다. 그 대신 아래 "같은 정렬선"(헤더·본문·푸터)과는 본문만 8
    어긋난다: 헤더·푸터는 자기 안에 면이 없어 원래 자리가 맞고, 본문은 면과 글이 섞이는
    자리라 글 쪽을 면에 맞춘 결과다.
  - 공용 부품이 갖는 값은 공통 층에 둔다 — `.klid-page-head`(shell.css) ·
    `.klid-result-head`·`.klid-result-count`(krds-theme.css). 그 화면에만 있는 맨바닥 줄은
    화면 CSS 가 같은 8 을 준다.
  - **맨바닥에 놓인 안내 띠(Alert)는 상자가 아니라 가장 왼쪽 잉크를 8 에 맞춘다** (2026-08-19 확정).
    Alert 은 면이라 상자가 0 에 서는데 바로 위 페이지 제목은 맨바닥 글이라 8 에 선다 — 상자와
    안쪽 내용 중 하나만 맞출 수 있고, **줄의 시작을 잡는 것은 첫 잉크다.** 보더 1 을 뺀 좌측 패딩
    7 을 주면 앞에 선 경고 아이콘이 정확히 8 에서 시작해 제목의 첫 글자와 한 선에 선다 (글은
    아이콘 뒤라 32). 아이콘 없이 글로 시작하는 띠도 같은 7 로 글이 8 에 선다.
    **면 안에 든 Alert 은 이 규칙 밖이다** — 폼 카드·상세 카드 안에서는 카드 패딩이 기준선이고,
    실제로 맨바닥에 띠를 놓는 화면은 내 학습데이터(SCREEN-013)뿐이다.
    아이콘과 글의 **세로** 정렬은 화면이 아니라 Alert 컴포넌트가 갖는다 (Alert.css).
- **라벨은 대상에 밀착** : 폼 라벨→입력 6, 입력→에러·카운터 줄 6, 제목→설명 4~6.
  즉 "붙어 있는 것"은 4~6px, "다음 항목"은 20px 부터. 그 사이 값(10~16)은 한 컴포넌트 안에서만 쓴다
  (KPI 아이콘칩→숫자 16 등).
- 콘텐츠 최대폭 1200 + 좌우 24 — **헤더·본문·푸터가 같은 정렬선(콘텐츠 시작점)을 공유한다**
  (2026-07-29 통일: 킷 기본 wrap 1248·푸터 패딩 리셋을 `--krds-contents-wrap-size: 120rem` +
  푸터 inner 패딩 복원으로 본문 체계에 맞춤). 페이지 배경 : 메인 흰색 / 서브페이지 canvas(#f4f6fa) —
  흰 카드가 도드라지는 전제라, 서브페이지에서 카드 보더를 생략하지 않는다.
- 그리드 간격 : 카드 16 · 컬럼 24 · KPI 세로 32.

---

## 7. 라운드 위계 (참조표)

| 라운드 | 대상 |
|---|---|
| 캡슐(최대) | 버튼·칩·정보 배지·검색창·탭(채움형) |
| 6 | 사각 태그 (출처·구성) |
| 8 | 입력·셀렉트·아이콘 버튼·페이저 셀 |
| 12 | KPI 아이콘 칩·셀렉트 팝업 |
| 16 | 카드·표 셸·모달·아코디언·빈 상태 |
| 24 | 큰 패널·미디어 |

명령(누르는 것)은 캡슐, 내용 상자는 8, 표면은 16. 하나의 요소에서 바깥 라운드보다 안쪽 라운드가
크면 안 된다 (카드 16 안에 KPI 칩 12, 그 안에 아이콘 — 안쪽으로 갈수록 작아진다).

### 둥근 면 위 맨 글자는 좌우 4 를 더 먹인다 (2026-08-25 확정)

라운드 12 이상의 면(카드·패널·표 셸) 위에 글자가 **바로** 얹히면, 여백 선에 정확히 맞아 있어도
눈에는 밖으로 새 보인다. 모서리가 안으로 말려 들어오면서 그 근처의 여백이 실제보다 좁게 읽히기
때문이다. 그래서 **가장자리에 닿는 맨 글자에만** 4 를 더한다 — 왼쪽에 붙으면 오른쪽으로 4,
오른쪽에 붙으면 왼쪽으로 4.

- **글자만 움직인다.** 배지·버튼·입력칸·썸네일은 제 면이 있어 그 경계에서 선이 그어지므로
  보정이 필요 없고, 제자리에 둔다. 그래서 배지 왼끝과 그 아래 제목 첫 글자가 4 어긋나 보이는
  자리가 생기는데 **그것이 의도다**.
- **가장자리에 실제로 닿는 글자만.** 가로형 목록 줄처럼 왼쪽이 썸네일·오른쪽이 버튼이면 그
  사이 글자는 어느 가장자리에도 안 닿는다 — 대상이 아니다. 가운데 정렬 글자도 마찬가지.
- **바깥쪽 덩어리에만 건다.** 안쪽 글자는 따라 들어온다. 겹쳐 걸면 8 이 된다.
- **캡슐(칩·배지)과 내용 상자(라운드 8)는 뺀다.** 칩은 완전히 둥근 대신 좌우 여백이 이미
  그 몫을 하고, 내용 상자는 여백이 라운드의 두 배라 새 보이는 현상이 없다.
- **표는 첫 열·끝 열이 통째로 움직인다** (§5 "표 끝 열 20" → 24). 열 하나만 반쯤 밀 수 없어
  그 칸의 배지도 같이 밀린다 — 「글자만」과 갈리는 유일한 예외다.

선언은 [src/styles/klid-optical.css](src/styles/klid-optical.css) 한 곳에 모여 있다. 값은
`--klid-text-optical` 토큰 하나이고, 면을 새로 만들면 그 파일 2 절 목록에 한 줄 보태면 된다.
한 자리에서 꺼야 하면 그 요소에 `data-optical="off"`.

---

## 8. 색 · 상태 (요약)

전체 팔레트와 역할 매핑 정본은 [src/styles/krds-theme-cobalt.css](src/styles/krds-theme-cobalt.css),
실물 확인은 스토리북 토큰 뷰어. 여기는 치수 작업 중 자주 쓰는 것만.
KRDS 밖 도메인 토큰(이벤트 유형색 8종·violet 증강 계열·canvas)은 klid-domain.css
(`--klid-` 프리픽스) — 스토리북 "토큰 > 컬러 · 도메인(KLID)" 에서 확인.

- 강조 단일색 코발트 600(#2e45dc). hover 700, active 800, 틴트 표면 50, 틴트 보더 200~500.
  KRDS 매핑: 600→primary-50 앵커. 목업 500(#4a61e8, 밝은 포인트 톤)은 표준 칸이 없어
  **primary-45 커스텀 슬롯**으로 보존 (krds-theme-cobalt.css).
- 텍스트 사다리 : 제목 slate-900 / 본문 800 / 표 셀 700 / 설명 600 / 메타·표 헤더·라벨 500.
  **흰 배경 의미 텍스트 하한은 500** (4.76:1 — KWCAG 4.5:1, 2026-07-29 대비 감사로 확정).
  **canvas(#f4f6fa) 배경 위 하한은 600** — 500 은 canvas 에서 4.4:1 로 미달한다 (브레드크럼 등).
  400 은 비활성(대비 예외 대상)·placeholder·순수 장식·aria-hidden 아이콘 전용.
- 보더 : 카드 slate-200, 입력 slate-300 (입력이 한 단계 진함 — 채워야 할 곳임을 알린다), focus cobalt-400.
- **opacity 색 배경 금지.** 배경은 단색 표면 토큰, 색은 보더·텍스트·아이콘에만. 예외는 모달
  스크림(slate-900/45)·미디어 오버레이·헤더 blur·다크 밴드 위 장식 넷뿐.
- focus 는 2px 아웃라인 + 2px 갭 (krds-focus.css 정본).
- 아이콘은 텍스트 색을 따른다. 크기는 역할 고정 : 배지 안 14 / 인라인 16 / 버튼·툴바 18 / 단독 20 / KPI 칩 22 / **타일 안 24**.
  타일(글자 없이 면 위에 글리프 하나만 서는 자리 — EventTypeTile 56 정사각)이 사다리의 끝이다.
  옆에 글자가 없어 글리프가 유형을 말하는 유일한 표시라, 칩 안 아이콘보다 한 단계 크게 선다 (2026-08-07 확정).

---

## 9. 반응형 범위 (2026-07-28 확정)

브레이크포인트는 KRDS 내장 기준을 그대로 쓴다. **767px 이하 모바일 / 768~1023 태블릿 / 1024px 이상 데스크톱.**
커스텀(.klid-*) CSS 도 이 두 지점만 쓰고 제3의 기준을 만들지 않는다. 태블릿은 별도 설계 없이
데스크톱 레이아웃의 유연한 축소로 처리한다.

화면은 3단으로 가른다:

| 단계 | 화면 | 모바일 동작 |
|---|---|---|
| 완전 반응형 | 메인 · 검색·데이터셋 상세(다운로드 신청 포함) · 소식&참여 · 이용안내 · 약관 · 회원(로그인·가입·계정찾기) · 마이페이지 | 전 기능 사용 가능 |
| 조회 전용 | 워크스페이스 대시보드 · 저작 목록 · 증강 진행 상태 | 조회는 가능, 생성·편집 액션은 숨기고 "PC 환경에서 이용" 안내로 대체 |
| 데스크톱 전용 | 라벨링 툴(저작 편집) · 관리자 콘솔 | 진입 시 차단이 아니라 안내 화면(+돌아가기 동선) |

- 판단 기준: 외부 사용자의 "찾아보고 · 신청하고 · 확인하는" 동선은 모바일 필수.
  정밀 편집·내부 관리는 대응 비용 대비 가치가 없다.
- **모션(히어로·이벤트 복셀 캐릭터)은 모바일에서도 재생한다.** 옵트아웃은 OS 의
  prefers-reduced-motion 에 위임 (buddy 의 useReducedMotion 훅이 정지 프레임 폴백,
  화면 밖 rAF 정지 구현 완료). 모바일이라는 이유로 일괄 비활성화하지 않는다.
- 조회 전용 화면의 액션 버튼은 뷰포트로 노출을 분기한다. 화면 조립 시 새 화면마다
  이 표에서 단계를 먼저 확인하고 시작한다.

### 좌우로 미는 콘텐츠의 가장자리 (모바일 · 2026-08-31 확정 · 2026-09-01 정리)

휴대폰에서 좌우로 미는 것 — **옆으로 미는 줄**(추천 세트 · 저장분 칩 · 상세 미리보기)과
**화면보다 넓은 표** — 은 가장자리에서 셋이 한꺼번에 서야 한다.

| 자리 | 좌우 여백 |
|---|---|
| **처음** | 왼쪽이 빈다 — 내용이 화면의 다른 것과 **같은 선**에서 시작한다 |
| **미는 동안** | 내용이 그 여백을 가로질러 **화면 좌우 끝까지** 찬다 |
| **마지막** | 오른쪽이 빈다 |

- 짜임은 하나다: 미는 상자가 좌우 여백을 **되돌려**(음수 마진) 화면 끝까지 나가고,
  그 여백을 **안쪽 패딩으로 되돌려 넣는다.** 멈춤(스냅)이 있는 줄은 scroll-padding 도 같이 준다.
- 되돌릴 폭은 **셸이 이름으로 준다** (`--klid-page-pad`). 화면이 16 을 옮겨 적지 않는다.
- 공용 클래스 `.klid-swipe-edge` (krds-theme.css) 하나뿐이다. **미는 자리가 생기면 이 클래스를
  붙인다** — 화면마다 같은 세 줄을 다시 적지 않는다.
- **표는 「미는 것이 카드」다.** 표 셸이 껍데기(면·보더·라운드)를 벗어 미는 상자가 되고,
  그 껍데기를 표를 감싼 안쪽 상자가 입는다. 그래서 처음 자리에 카드 왼쪽 둥근 모서리가,
  마지막 자리에 오른쪽 모서리가 선다 — 미리보기 줄에서 낱장이 하는 일과 같다.
  껍데기를 셸에 남겨 두면 카드가 제자리에 박혀 「미는 동안」과 「마지막」이 서지 않는다.
- 옆에 더 있다는 표식은 두 가지다 : 미는 줄은 **잘린 낱장**(카드 폭을 화면에 딱 떨어지지 않게
  잡는다), 표는 **늘 뜨는 막대**(열 폭이 제각각이라 늘 걸치게 잡을 수가 없다 · §5).

---

## 10. KRDS 적용 시 사이즈 판단 순서

1. **높이부터 스케일에 스냅한다.** 대상 KRDS 사이즈의 높이를 36/44/52 중 가까운 값으로 재정의
   (버튼·입력·셀렉트는 krds-theme.css 에서 이미 완료 — small→36, medium→44, large→52 대응).
2. 글자는 높이의 1/3 근처 스케일 토큰(14/15/17)으로.
3. 좌우 패딩은 요소 성격으로 가른다 : 버튼류(누진 0.39~0.54h) / 입력류(14 고정) /
   캡슐 텍스트(가로:세로 비율 §4).
4. KRDS 토큰이 사이즈 구분 없는 단일값이면 md 앵커. 토큰 하나가 여러 자리를 겸해 절충이 필요하면
   (예: 아코디언 padding 단일 토큰 → 세로 16 기준 통일, 좌우는 기준 20 대비 -4px) 절충 내용을
   krds-theme.css 주석으로 남긴다.
5. 도출값이 어색해 보이면 수치를 더 만지지 말고 스토리북 렌더에서 원인을 찾는다.

---

## 11. 헤더 · GNB (2026-07-29 확정)

구조 정본은 KRDS 피그마 헤더 유형 중 **결합형(Type=union)**: 로고 · GNB · 계정 버튼 · 전체메뉴가 한 줄.
셸(.klid-header)은 여기에 blur 배경과 GNB 중앙 정렬(결합형 기본은 우측)을 얹는다. 1024 미만은 킷 모바일 드로어.
타이포·상호작용은 krds-theme.css §Header·MainMenu 가 전역 적용 (스토리·셸 공통).

| | 값 |
|---|---|
| 컨테이너 | 최대 1200 · 좌우 24 · 상하 패딩 12 (총 높이 ~64) |
| GNB 항목 | 15/500 · 높이 40 · 좌우 패딩 14 · 라운드 8 · 간격 4 |
| GNB 색 | 기본 gray-60 · hover gray-5 필 · 활성 primary-60 텍스트 (밑줄 없음) |
| 계정 버튼 | 36 완전 pill · 14/600 (로그인 text · 회원가입 primary) |
| 전체메뉴 버튼 | 36 정사각 아이콘 (데스크톱 ≡ = IconButton lg · 모바일 킷 트리거도 동일 룩, 라벨은 sr 전용) |
| 패널 공통 (드롭다운·사이트맵) | 제목 17/600 + 하단 밑줄(패딩 12) · 항목 15 gray-50 · 불릿 없음 · hover primary-60 텍스트 · 그림자 0 20 25 -5 slate-900 10% |
| 사이트맵 오버레이 | 컨테이너 1200 · 패딩 32/24/40 · 5컬럼 gap 32 · 계정 aside 224(좌 보더 + 패딩 40) · 딤 slate-900/30 |

값 출처: /style 헤더·전체메뉴 실측 + KRDS 피그마 Header 페이지(결합형 구조·유형 정의).

## 12. 좌측 LNB (2026-08-04 확정)

패널 폭 240 · 접으면 64 아이콘 레일 · 1024 미만 숨김. 사용자 카드 + 메뉴 카드 두 장(카드 라운드 16 · 패딩 10 · 사이 16).

| | 값 |
|---|---|
| 1단 항목 | 15/500 · 패딩 10/12 · 라운드 12 · 아이콘 18 · 아이콘↔글 간격 10 · 항목 사이 2 |
| 1단 색 | 기본 gray-60 · hover gray-5 필 · 현재 primary-5 필 + primary-60 텍스트 600 |
| 2단 항목 | 14 · 패딩 8/12 · **왼쪽 40** · 아이콘 없음 |
| 2단 번호 (순서가 있을 때) | 13/600 gray-50 · 폭 18(아이콘 자리) · 현재면 라벨 색을 따른다 |
| 2단 부모 (하위가 현재일 때) | 배경 없이 gray-90 600 — 배경 필은 실제 현재 항목만 가진다 |
| 접힘 레일 | 2단 목록은 숨김 (레일에는 1단만) |

**2단 규칙**
- 2단은 **한 화면 안의 목차를 메뉴로 올릴 때만** 쓴다. 화면이 갈라지면 그건 1단이다.
- 2단에는 아이콘을 붙이지 않는다. 들여쓰기가 위계를 말하고, 아이콘이 붙으면 1단과 무게가 같아진다.
- 왼쪽 40 은 임의값이 아니라 **부모 라벨이 시작하는 자리**(패딩 12 + 아이콘 18 + 간격 10)다. 글자 머리가 부모와 한 줄로 선다.
- 2단은 항상 펼쳐 둔다. 접었다 펴는 동작은 안에 뭐가 있는지 감추기만 한다.
- 화면 안 탭과 2단 메뉴를 같이 쓰지 않는다. 좌측 메뉴가 이미 면 전환을 맡고 있어 위계가 이중이 된다.
- 하위가 **나란한 주제가 아니라 순서**면 라벨 앞에 번호를 둔다. 번호는 아이콘 자리(18)를 대신 쓴다 — 아이콘을 붙이면 1단과 무게가 같아지지만, 번호는 순서를 말할 뿐이라 무게가 덜하다.
- 순서 있는 하위는 본문 맨 아래에 **앞·다음 이동(StepPager)** 을 둔다. 메뉴만으로는 한 칸을 다 읽고 나서 갈 곳이 없다.

브레드크럼은 홈 > 영역 > (2단이면 부모) > 현재 순. 페이지 제목(h1)은 항상 현재 항목 이름이다.

---

## 13. AI × 재난유형 마크 (2026-08-07 확정)

"이 유형을 다루는 AI" 를 글리프 하나로 말하는 마크. 둥근 사각(AI)의 **우측 상단이 열리고 그
자리에 재난유형 글리프**가 선다. 화면용은 컴포넌트
[EventAiMark](src/components/custom/EventAiMark.tsx)(스토리북 포털/EventType/AImark),
같은 그림의 독립 파일이 `public/ai-mark-*.svg` 8종이다 — 기획·디자인 산출물, 피그마 반입용.
**짜임 정본은 [ai-mark-geometry.ts](src/components/custom/ai-mark-geometry.ts) 하나**이고,
컴포넌트와 [scripts/make-ai-marks.mjs](scripts/make-ai-marks.mjs) 가 그것을 같이 읽는다 —
값을 바꾸면 스크립트를 다시 돌려 파일 8종을 맞춘다. 파일은 **직접 고치지 않는다.**

유형 글리프와 색은 **`EventTypeIcon` 과 같은 것**을 쓴다 (event-glyphs.ts · `--klid-color-event-*`).
마크에서 따로 고르면 같은 유형이 배지와 마크에서 다른 그림이 된다.

| | 값 (24 격자) |
|---|---|
| **획** | **1.25 하나** — 사각·"AI"·글리프가 모두 같다 (2026-08-10 통일) |
| 사각 | (2,6) 에서 16×16 · 모서리 반경 3 · 윗변 11.883 에서 끊고 오른변 12.117 에서 이음 |
| 노치 (트인 구간) | 우측 상단 모서리에서 위·오른쪽 **같은 거리 5.492** · 글리프 잉크 반지름 + 여백 1.6 |
| 유형 글리프 | 한 변 11.5 · 획 1.25 (`stroke-width` 2.609 = 1.25 × 24/11.5) · 잉크 중심을 모서리에서 대각선 바깥으로 1 띄운 점에 맞춤 |
| AI 글자 | Roboto 800 · 크기 7.6 **× 1.1** · 자간 0.3 · **패스로 굳힘** · 모서리 반경 0.5 · 세로획 1.324 |
| 뷰박스 | `0.896 -0.896 24 24` — 24 칸을 **옮긴 것**이지 키운 것이 아니다 (아래 규칙) |
| 크기 | md 20 (기본) · lg 22 · xl 32 · **hero 220** (자리에 따라 도출 — 아래) |

**규칙**
- **모든 정렬의 기준은 사각의 우측 상단 모서리 한 점**이다. 노치와 글리프가 그 점을 같이
  중심으로 삼아야 둘이 맞물려 보인다. 셋(사각·노치·글리프)은 서로 물려 있으니 하나만 고치지 않는다.
- **획은 1.25 하나다 (2026-08-10 확정).** 사각 1.8 · 글리프 1.25 · "AI" 세로획 1.204 로 셋이
  갈려 있었다. 사각만 혼자 굵어 "굵은 상자에 얇은 그림이 붙은 것" 으로 읽힌다.
  - **통일 방향은 사각을 얇게 하는 쪽이다.** 반대로 글리프를 1.8 로 굵히는 것은 2026-08-07 에
    해 봤다가 되돌린 값이다 — lucide 는 24 격자에 획 2 로 그리므로(1:12) 11.5 로 줄인 글리프에
    1.8 을 주면 비율이 1:6.4 가 되어 주먹·맥박처럼 안쪽 선이 촘촘한 글리프가 덩어리로 뭉갠다.
    **글리프의 1.25 가 이 마크에서 맞는 값이고, 사각이 거기에 맞춰 내려온다.**
  - **"AI" 는 1.1 배로 키운다.** 사각 안이 비어 보였고, 키우면 세로획이 1.204 → **1.324** 가
    되어 같은 급으로 올라온다. 서체 윤곽선이라 획이 자리마다 다르다 (세로획 1.324 ·
    A 사선 1.12 · 가로대 1.03) — 서체의 성질이라 없앨 수 없고, 세로획이 대표값이다.
  - **끊는 자리는 획이 얇아진 만큼 바깥으로 민다.** 라운드캡이 끝점에서 획의 절반만큼 더
    나가므로, 굵기만 줄이면 트인 폭이 0.55 벌어져 셋이 물린 짜임이 풀린다. 윗변 11.608 →
    **11.883**, 오른변 12.392 → **12.117** 로 밀어 **눈에 보이는 가장자리(12.508 · 11.492)를
    그대로 둔다.** 획을 다시 건드리면 이 두 값도 같이 다시 잡는다.
  - ※ "AI" 잉크 가로중심이 9.503 이라 사각 정중앙(10)에서 왼쪽으로 0.497 비켜 있다. 노치가
    우측 상단에 트여 무게가 그쪽으로 쏠리므로 그대로 둔다 — 옮기려면 별도 결정이 필요하다.
- 글리프는 격자 중심(12,12)이 아니라 **잉크 중심**으로 얹는다. lucide 는 잉크를 격자 한가운데
  놓지 않아(세로 중심이 유형별로 12 또는 12.5), 격자 기준으로 맞추면 유형마다 어긋난다.
- **20 아래로 내리지 않는다.** 글리프 둘이 한 칸에 드는 마크라 배지 치수(14·16)에서는 "AI" 가
  뭉개진다. 배지 자리는 `EventTypeIcon` 이 계속 맡는다 (§8 아이콘 크기 규칙).
- **`hero` 는 자리가 크기를 정할 수 있다 (2026-08-10).** 220 은 기본값이고, 그림 한 점으로 서는
  자리에서는 **사각을 그 자리의 선에 맞춰** 크기를 도출한다. 맞추는 것은 마크 전체가 아니라
  **둥근 사각**이다 — 사각 바깥 높이가 24 격자에서 **17.25** (윗변 6 − 0.625 … 아랫변 22 + 0.625)
  이므로, 사각을 높이 H 에 맞추려면 마크가 **H × 24/17.25**, 아래끝은 뷰박스가 사각 아래로 더
  갖는 **0.479 칸**만큼 내린다. AI 모델 상세 밴드가 첫 자리다 (제목 윗선 ~ 실행 버튼 밑선.
  1280 에서 H 199 → 277, 1024 에서 H 223 → 310). 노치 밖 글리프는 그 선 위로 서므로
  **자리가 모자라면 잘리는 것을 받아들인다** — 자리 쪽에서 자른다.
  - **크기를 도출하는 자리는 데스크톱 전용이다.** 폭이 줄면 마크가 설 자리가 안 나오는데,
    크기를 고정해 피하면 이 배치의 근거인 격자 정렬 자체가 사라진다. **줄이지 말고 내린다** —
    AI 모델 상세는 1024 미만에서 마크를 내리고 글자리도 함께 걷는다 (2026-08-10).
- "AI" 는 `<text>` 가 아니라 **굳힌 패스**다. 서체가 없는 환경(svg 파일을 `<img>`·css background 로
  쓰거나 폰트가 아직 안 붙은 첫 페인트)에서 폴백 서체로 갈리지 않게 하기 위해서다.
  **테마 서체를 바꿔도 여기는 안 따라온다** — 그때는 패스를 다시 뽑아야 한다.
- **사각·글자·글리프가 모두 유형색 한 가지다.** 색은 root 의 `color`(`--klid-event-color`) 한
  곳에만 걸고 안쪽 셋은 전부 `currentColor` 로 받는다. 채움 위에 얹히는 자리에서는 그 변수를
  `currentColor` 로 덮으면 마크 전체가 흰색이 된다 (`EventTypeIcon` 과 같은 방식).
  - **바탕으로 서는 자리는 그라데이션으로 칠할 수 있다 (2026-08-10).** 읽는 표시가 아니라 면에
    남은 자국인 자리에 한한다. 셋이 전부 `currentColor` 라 `[stroke='currentColor']` ·
    `[fill='currentColor']` 로 조준해 `url(#…)` 로 바꾼다 (문서 순서로 잡지 말 것 — 조각이
    하나 늘면 어긋난다). 그라데이션은 **`gradientUnits="userSpaceOnUse"` + 마크 뷰박스 좌표**
    여야 한 축이 마크 전체를 지나간다. 기본값이면 조각마다 제 상자에서 처음부터 끝까지 칠해진다.
    ※ 글리프는 중첩 `<svg>` 라 제 좌표계를 가져 축이 그 안에서 다시 시작한다. **두 끝을 팔레트
      한 단계 안으로 두면** 칠해지는 값이 그 사이를 못 벗어나 한 결로 읽힌다 — 멀리 벌릴 때는
      글리프용 좌표를 `GLYPH_X`·`GLYPH_Y` 로 역산해 따로 줘야 한다.
    AI 모델 상세 밴드가 첫 자리다 (왼위 `primary-95` → 오른아래 `primary-90`).
- `public/ai-mark-*.svg` 는 색이 hex 로 박혀 있다. **화면에서 쓰지 않는다** — 유형색 토큰도
  흰색 반전도 안 따라온다. 기획·디자인 산출물, 피그마 반입용이다.
- **뷰박스는 0 이 아니라 (0.896, −0.896) 에서 연다 (2026-08-10 실측·수정).** 글리프를 사각
  모서리 **바깥**에 얹는 짜임이라 글리프 획(1.25)의 절반이 24 칸을 넘는다 — 위로는 폭행 −0.417 ·
  화재/산불 −0.178, 오른쪽으로는 납치·쓰러짐·침수·산사태 24.417. 0 에서 열면 그만큼 잘려
  불꽃 끝이 납작해지고 납치의 X 오른팔이 끊긴다.
  - **칸을 옮기기만 하고 키우지 않는다.** 8종 잉크 합집합이 1.375~24.417 × −0.417~22.625 =
    **23.042 정사각**이라, 24 칸 한가운데 놓으면 사방 여백이 0.479 로 같아진다. 24 칸이 그대로라
    배율도 그대로다 — md 20 에서 한 칸은 여전히 20/24 이고 획(1.25)의 비례가 안 바뀐다.
  - **글리프를 안으로 당겨서 맞추지 말 것.** 잉크 중심을 모서리에 맞춘 좌표가 깨지고
    노치와의 맞물림이 풀린다. 기하는 그대로 두고 **보는 창만 옮긴다.**
  - `overflow: visible` 로 풀지 않는다. 잉크가 마크의 자리 밖으로 나가 이웃과 겹치고,
    `<img>` 로 쓰는 `public/ai-mark-*.svg` 는 어차피 안 고쳐진다 (이미지 상자에서 잘린다).
  - 컴포넌트와 파일 8종이 같은 값을 쓴다. 글리프·획·크기를 건드리면 **이 값을 다시 재서 고친다.**

## 14. 재난유형 아트 (2026-08-10 확정)

유형을 **면과 색으로** 말하는 큰 그림. 자리 하나가 어느 유형에 대한 자리인지 글자보다 먼저
읽히게 한다. 화면용은 컴포넌트 [EventTypeArt](src/components/custom/EventTypeArt.tsx)
(스토리북 포털/EventType/filled), 그림 파일은 `public/event-icon-*.svg` 8종이다.
파일은 [scripts/make-event-icons.mjs](scripts/make-event-icons.mjs) 가 만든다 — **직접 고치지 않는다.**

글리프와 색은 **`EventTypeIcon` 과 같은 것**을 쓴다 (event-glyphs.ts · `--klid-color-event-*`).
아트에서 따로 고르면 같은 유형이 배지와 아트에서 다른 그림이 된다.

| | 값 |
|---|---|
| 캔버스 | 256 정사각 · 배경 투명 |
| 글리프 | **잉크 상자와 칠한 면적을 반반 섞어** 유형마다 배율이 다르다 (6.43~7.91배) |
| 정렬 | 격자 중심이 아니라 **잉크 상자 중심**을 캔버스 중앙에 얹는다 |
| 면 그라데이션 | 좌상 → 우하. 밝은 끝 명도 0.68 · 어두운 끝은 유형색(명도 하한 0.36) |
| 색상 이동 | 밝은 끝만 **14°** (따뜻한 계열은 45°, 차가운 계열은 180° 방향) |
| 발광 | 유형색 방사 halo · 반지름 86 · 번짐 12 |
| 크기 | md 64 (카드 안) · lg 96 (단독) · xl 128 (히어로) |

**규칙**
- **일률 배율도, 상자만 맞추는 것도 안 된다.** 두 실측이 서로 다른 답을 낸다.

  | 재는 것 | 벌어진 폭 |
  |---|---|
  | 잉크 상자 기하평균 √(너비×높이) | 화재 16.31 ~ 유괴 19.49 (19%) |
  | 칠한 면적의 √ | 유괴 82.4 ~ 교통사고 113.8 (38%) |

  격자가 같아도 잉크가 격자를 채우는 정도가 다르고(→ 일률 배율 실패), 상자가 같아도 안이
  얼마나 차 있는지가 다르다(→ 상자만으로도 실패). 상자만 맞추면 속이 빈 유괴가 작아 보이고,
  면적만 맞추면 유괴가 혼자 커진다. **면적 보정에 제곱근을 씌워 반씩 섞는다.**
  최대변으로 맞추는 안도 재 봤지만 물결(넓고 낮음)·나무(좁고 높음)처럼 한쪽으로 긴 글리프가
  면적으로 커 보여 접었다.
  두 값은 스크립트 TYPES 의 `box`·`ink` 에 실측으로 박혀 있다 — **글리프나 획 굵기를 바꾸면
  다시 재야 한다.** 재는 법은 스크립트 주석에 있다 (브라우저 getBBox + 캔버스 알파 픽셀 계수.
  node 에는 SVG 레이아웃이 없어 헤드리스가 필요하다).
- **마지막 한 급은 눈이 정한다 (2026-08-10 확정).** 재는 것으로는 여기까지고, 형태가 주는
  무게는 면적에 안 잡힌다 — 계산이 고르다고 한 뒤에도 물결이 커 보이고 나무가 작아 보였다.
  스크립트 `NUDGE` 에 유형별 px 보정을 둔다. **폭력이 기준이고 나머지는 그것 대비로 움직인다.**
  계산으로 유도하지 말 것 — 화면 검토에서 정하는 값이다.

  | | 화재 | 침수 | 산사태 | 교통사고 | 쓰러짐 | 폭력 | 산불 | 유괴 |
  |---|---|---|---|---|---|---|---|---|
  | 크기 | 129 | 115 | 127 | 117 | 122 | **120** | 137 | 137 |
  | 보정 | 0 | −12 | 0 | 0 | 0 | −2 | +6 | 0 |

  (글리프가 차지하는 정사각 px · 256 캔버스 기준. 스크립트를 돌리면 이 표가 그대로 찍힌다)
- **그라데이션 방향은 좌상 → 우하 대각이 기본이고, 침수만 좌 → 우로 눕힌다** (2026-08-10 확정).
  다른 일곱은 덩어리라 대각이 빛의 방향으로 읽히는데, 물결은 가로로 긴 획 셋이라 대각을 걸면
  **줄마다 아래쪽이 어두워져 획이 두꺼워 보인다.** 한 방향으로 두면 세 줄이 같은 자리에서 같이
  어두워져 한 덩어리로 읽힌다. 방향은 스크립트 TYPES 의 `grad` 로 유형마다 열려 있다.
- **아트가 캔버스의 절반쯤만 차지한다** (평균 126/256 ≈ 49%). 나머지는 발광이 퍼질 자리다.
  쓰는 쪽에서 그림을 크게 세우려면 **상자를 한 급 올리고 남는 여백을 음수 마진으로 걷어낸다** —
  자리는 그대로 두고 그림만 커진다. 학습데이터 검색의 추천 검색 세트 카드가 그 방식이다
  (xl 128 + margin −32 → 자리 64 · 그림 63). 크기를 바꾸면 그 마진도 같이 다시 잰다.
- **56 아래로 내리지 않는다.** 발광과 면 그라데이션이 뭉개져 그냥 색 실루엣이 된다.
  그 아래는 `EventTypeIcon` 이 계속 맡는다 (§8 아이콘 크기 규칙).
- **색상 이동은 14° 를 넘기지 않는다.** 24° 로 뽑았을 때 침수(#0284c7)와 교통사고(#0d9488)가
  같은 시안이 되어 두 유형이 섞였다. 유형 판별은 어두운 끝(유형색 자체)이 맡는다.
- **어두운 유형색은 채도를 낮춰서 밝힌다.** 브라운(산사태 #92400e)을 채도 그대로 밝히면
  금색이 되어 다른 유형처럼 보인다. 노랑(쓰러짐)은 반대로 **명도를 내리면 갈색이 된다** —
  면 안에 파낸 표시(심박선)를 유형색보다 한 톤만 아래로 두는 이유다.
- **발광 반지름 86 이 상한이다.** 더 키우면 번짐이 캔버스 밖에서 잘려 네모난 테가 진다.
- **배경은 틴트 면(회색·보라 계열 5~10)을 고른다.** 발광은 어두운 면에서 가장 살고 흰 면에서는
  거의 남지 않는다. 흰 카드 위에 올리면 그라데이션 면만 남는다.
  - **흰 면 위에 그냥 두는 것도 갈래로 인정한다 (2026-08-10 추가).** 발광을 포기하고 면
    그라데이션만 쓰는 자리다. 그 상태로도 여덟 유형은 갈린다 — 유형 판별은 어두운 끝(유형색
    자체)이 맡기 때문이다. 아트 하나를 위해 카드에 유형색 면을 깔면 그 카드가 이웃한 흰 카드
    줄에서 혼자 세게 서는데, 곁가지 자리(추천·연관)에서는 그쪽이 더 나쁘다.
    학습데이터 검색(SCREEN-004)의 추천 검색 세트 카드가 이 경우다.
    **틴트 면을 깔 수 있는 자리에서는 까는 것이 여전히 기본이다** — 발광까지 살아야 아트가
    제 값을 한다.
- 그림 안에 발광 여백이 이미 들어 있다. **바깥에서 여백을 더 주지 않는다** — 유형끼리 간격이
  벌어져 목록에서 줄이 안 맞는다. 자리 여백은 쓰는 쪽 레이아웃이 갖는다.
- lucide 는 선 아이콘 세트라 면 변형이 없다(1,997 종 중 닫힌 도형만으로 된 것은 143 종, 전부
  기하 도형). 그래서 획을 면으로 바꾸는 처리를 스크립트가 한다 — 닫힌 도형은 채우고, 열린 획은
  굵은 획으로 남기고, 면 위에 얹히는 것은 그림자로 층을 나눈다. **글리프를 바꾸면 그 배분을
  다시 정해야 한다.**
- 폭력(`hand-fist`)은 엄지를 감아 도는 획이 손바닥을 가로질러, 채우면 손바닥 안쪽이 쐐기로
  파인 채 남는다. **지금은 파인 채로 둔다** — 메우면 주먹의 안쪽 구조가 사라져 돌기 달린
  덩어리가 된다. 다만 파인 자리를 두 판(파인 판·메운 판) 중 어느 쪽으로 갈지는 **미결**이다.
  메우려면 스크립트 fill 앞에 손 윤곽 안쪽 메움 도형을 한 장 더 깔면 된다.
